spring.application.name=amanora-tracker
server.port=8080

# --- Update these for your local/deployed PostgreSQL ---
spring.datasource.url=jdbc:postgresql://localhost:5432/amanora_tracker
spring.datasource.username=postgres
spring.datasource.password=kkfire123

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.sql.init.mode=always
spring.jpa.properties.hibernate.format_sql=true

# Allow the Next.js frontend (localhost:3000 in dev) to call this API
spring.mvc.cors.allowed-origins=*
